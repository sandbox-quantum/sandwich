load("//common/rules/mkdocs:rules.bzl", "mkdocs_tree")

mkdocs_tree(
    name = "mkdocs",
    srcs = glob(
        ["*"],
        exclude = ["BUILD.bazel"],
    ),
    path = "images",
    visibility = ["//visibility:public"],
)
